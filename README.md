Binarization is a preprocessing technique used to convert numerical data into binary values, usually 0 and 1, based on a specified threshold.

Binarization is useful when you don't need the exact numerical value, 
but only need to know whether a value is above or below a certain threshold

Binarization converts numerical features into binary values, usually 0 and 1, based on a specified threshold.
